// STUB — replaced by the persistence/admin workstream (Agent C).
export default function AdminLogin() {
  return <div className="min-h-screen flex items-center justify-center text-slate-500">Admin login — coming online…</div>;
}
