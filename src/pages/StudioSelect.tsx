// STUB — replaced by the studio-shell workstream (Agent A).
export default function StudioSelect() {
  return <div className="min-h-screen flex items-center justify-center text-slate-500">Design Studio — coming online…</div>;
}
