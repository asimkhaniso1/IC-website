import{useCallback,useEffect,useMemo,useState,type FormEvent}from'react';import{CheckCircle2,Clock3,FileCheck2,Loader2,XCircle}from'lucide-react';import FactoryGuard from'./FactoryGuard';import FactoryShell from'./FactoryShell';import{advancePurchaseRequest,listPendingApprovals,rejectPurchaseRequest}from'./api';const field='w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm';const stages=['PREPARED','CHECKED','PREAPPROVED','APPROVED'];
function Body(){const[rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true),[busy,setBusy]=useState(''),[error,setError]=useState(''),[notice,setNotice]=useState('');const load=useCallback(async()=>{setLoading(true);setError('');try{setRows(await listPendingApprovals())}catch(e){setError(e instanceof Error?e.message:'Could not load approvals.')}finally{setLoading(false)}},[]);useEffect(()=>{void load()},[load]);const total=useMemo(()=>rows.reduce((n,r)=>n+(r.lines??[]).reduce((v:number,l:any)=>v+Number(l.quantity||0)*Number(l.rate||0),0),0),[rows]);async function run(key:string,action:()=>Promise<unknown>,message:string){setBusy(key);setError('');setNotice('');try{await action();setNotice(message);await load()}catch(e){setError(e instanceof Error?e.message:'Approval action failed.')}finally{setBusy('')}}function approve(e:FormEvent<HTMLFormElement>,r:any){e.preventDefault();const d=new FormData(e.currentTarget);void run(r.purchase_request_id,()=>advancePurchaseRequest(r.purchase_request_id,String(d.get('name')),String(d.get('notes')||'')),'Approval stage recorded.')}function reject(e:FormEvent<HTMLFormElement>,r:any){e.preventDefault();const d=new FormData(e.currentTarget);void run(r.purchase_request_id,()=>rejectPurchaseRequest(r.purchase_request_id,String(d.get('reason'))),'Purchase request rejected.')}return <div className="space-y-5"><div><p className="text-xs font-bold text-brand-600 uppercase tracking-[.18em]">Controlled workflow</p><h1 className="text-2xl font-bold mt-1">Approval Inbox</h1><p className="text-sm text-slate-500 mt-1">Review pending purchase requests and record accountable approval decisions.</p></div>{error&&<div className="p-3 bg-red-50 text-red-700 rounded-xl text-sm">{error}</div>}{notice&&<div className="p-3 bg-emerald-50 text-emerald-700 rounded-xl text-sm flex gap-2"><CheckCircle2 className="w-4 h-4"/>{notice}</div>}<div className="grid sm:grid-cols-3 gap-3">{[['Pending requests',rows.length],['Pending value',total.toLocaleString(undefined,{maximumFractionDigits:2})],['Oldest',rows[0]?.request_date??'—']].map(([label,value])=><div key={label} className="bg-white border rounded-2xl p-4"><p className="text-xs text-slate-500">{label}</p><p className="text-2xl font-bold mt-1">{value}</p></div>)}</div>{loading?<div className="p-24 flex justify-center"><Loader2 className="animate-spin text-brand-600"/></div>:!rows.length?<div className="bg-white border rounded-2xl p-16 text-center text-slate-400"><FileCheck2 className="mx-auto mb-3 w-8 h-8"/><p className="font-semibold">Approval inbox is clear.</p></div>:rows.map(r=><article key={r.purchase_request_id} className="bg-white border rounded-2xl overflow-hidden"><div className="p-5 border-b flex flex-wrap justify-between gap-3"><div><div className="flex gap-2 items-center"><b className="font-mono text-sm">{r.purchase_request_no}</b><span className="rounded-full bg-amber-50 text-amber-700 px-2 py-1 text-[10px] font-bold">{r.status.replaceAll('_',' ')}</span></div><h2 className="font-bold mt-2">{r.purpose}</h2><p className="text-xs text-slate-500 mt-1">{r.department} · Requested by {r.requested_by} · {r.request_date}</p></div><Clock3 className="text-amber-500"/></div><div className="p-5 grid xl:grid-cols-[1.4fr_1fr] gap-5"><div><div className="grid grid-cols-4 gap-2 mb-4">{stages.map((s,i)=>{const a=r.approvals?.find((x:any)=>x.approval_level===s);return <div key={s} className={`rounded-xl p-2 border ${a?'bg-emerald-50 border-emerald-100':'bg-slate-50'}`}><p className="text-[9px] font-bold">{i+1}. {s}</p><p className="text-xs mt-1 truncate">{a?.approver_name??'Pending'}</p></div>})}</div><div className="overflow-x-auto"><table className="w-full text-xs"><thead className="bg-slate-50 text-left"><tr><th className="p-2">Item</th><th className="p-2">Qty</th><th className="p-2">Rate</th><th className="p-2">Amount</th></tr></thead><tbody>{r.lines?.map((l:any,i:number)=><tr key={i} className="border-t"><td className="p-2">{l.item_description}</td><td className="p-2">{Number(l.quantity).toLocaleString()} {l.uom_text}</td><td className="p-2">{l.rate==null?'—':Number(l.rate).toLocaleString()}</td><td className="p-2 font-bold">{l.rate==null?'—':(Number(l.quantity)*Number(l.rate)).toLocaleString()}</td></tr>)}</tbody></table></div></div><div className="space-y-3"><form onSubmit={e=>approve(e,r)} className="rounded-xl bg-emerald-50 p-3 space-y-2"><p className="text-xs font-bold text-emerald-800">Approve next stage</p><input required name="name" placeholder="Approver name" className={field}/><input name="notes" placeholder="Approval notes" className={field}/><button disabled={!!busy} className="w-full rounded-xl bg-emerald-600 text-white py-2.5 text-sm font-bold flex justify-center gap-2">{busy===r.purchase_request_id?<Loader2 className="w-4 h-4 animate-spin"/>:<CheckCircle2 className="w-4 h-4"/>}Approve</button></form><form onSubmit={e=>reject(e,r)} className="rounded-xl bg-red-50 p-3 space-y-2"><p className="text-xs font-bold text-red-800">Reject request</p><input required name="reason" placeholder="Mandatory rejection reason" className={field}/><button disabled={!!busy} className="w-full rounded-xl border border-red-200 bg-white text-red-700 py-2.5 text-sm font-bold flex justify-center gap-2"><XCircle className="w-4 h-4"/>Reject</button></form></div></div></article>)}</div>}
export default function ApprovalInbox(){return <FactoryGuard><FactoryShell><Body/></FactoryShell></FactoryGuard>}
