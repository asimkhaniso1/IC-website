import{useCallback,useEffect,useMemo,useState,type FormEvent}from'react';import{AlertCircle,CheckCircle2,Loader2,PackageCheck}from'lucide-react';import FactoryGuard from'./FactoryGuard';import FactoryShell from'./FactoryShell';import{listPackingData,packFinishedGoods}from'./api';const field='w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm';
function Body(){const[data,setData]=useState<any>({batches:[],records:[],finished:[]}),[busy,setBusy]=useState(false),[error,setError]=useState(''),[notice,setNotice]=useState('');const load=useCallback(async()=>{try{setData(await listPackingData())}catch(e){setError(e instanceof Error?e.message:'Could not load packing.')}},[]);useEffect(()=>{void load()},[load]);const total=useMemo(()=>data.finished.reduce((n:number,x:any)=>n+Number(x.quantity),0),[data]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=e.currentTarget,d=new FormData(f);setBusy(true);setError('');try{await packFinishedGoods({p_batch:d.get('batch'),p_quantity:Number(d.get('quantity')),p_rolls:d.get('rolls')?Number(d.get('rolls')):null,p_cartons:d.get('cartons')?Number(d.get('cartons')):null,p_roll_length:d.get('roll_length')?Number(d.get('roll_length')):null,p_packed_by:d.get('packed_by'),p_remarks:d.get('remarks')||null});f.reset();setNotice('Packing completed and finished goods posted.');await load()}catch(e){setError(e instanceof Error?e.message:'Could not complete packing.')}finally{setBusy(false)}}return <div className="space-y-5"><div><p className="text-xs font-bold text-brand-600 uppercase tracking-[.18em]">Quality released output</p><h1 className="text-2xl font-bold mt-1">Packing & Finished Goods</h1><p className="text-sm text-slate-500 mt-1">Convert QC-approved WIP into available finished stock.</p></div><div className="grid grid-cols-3 gap-3">{[['Ready to pack',data.batches.length],['Packing records',data.records.length],['Finished meters',total.toLocaleString()]].map(x=><div className="bg-white border rounded-2xl p-4" key={String(x[0])}><p className="text-xs font-bold text-slate-500">{x[0]}</p><p className="text-2xl font-bold mt-2">{x[1]}</p></div>)}</div>{error&&<div className="p-3 bg-red-50 text-red-700 rounded-xl flex gap-2"><AlertCircle className="w-4 h-4"/>{error}</div>}{notice&&<div className="p-3 bg-emerald-50 text-emerald-700 rounded-xl flex gap-2"><CheckCircle2 className="w-4 h-4"/>{notice}</div>}<form onSubmit={submit} className="bg-white border rounded-2xl p-5 space-y-4"><div className="flex gap-2"><PackageCheck className="text-brand-600"/><h2 className="font-bold">New packing record</h2></div><div className="grid md:grid-cols-3 gap-2"><select required name="batch" className={field}><option value="">QC-approved batch…</option>{data.batches.map((x:any)=><option key={x.production_batch_id} value={x.production_batch_id}>{x.batch_code} · {x.order?.product?.description} · {x.actual_quantity} m</option>)}</select><input required name="quantity" type="number" min=".01" step=".01" placeholder="Packed quantity (meters)" className={field}/><input name="roll_length" type="number" min=".01" step=".01" placeholder="Roll length" className={field}/><input name="rolls" type="number" min="0" placeholder="Roll count" className={field}/><input name="cartons" type="number" min="0" placeholder="Carton count" className={field}/><input required name="packed_by" placeholder="Packed by" className={field}/><input name="remarks" placeholder="Remarks" className={`${field} md:col-span-3`}/></div><div className="text-right"><button disabled={busy||!data.batches.length} className="px-5 py-2.5 rounded-xl bg-emerald-600 text-white text-sm font-bold disabled:opacity-40">{busy?<Loader2 className="animate-spin w-4 h-4"/>:'Complete packing'}</button></div></form><section className="bg-white border rounded-2xl overflow-hidden"><h2 className="font-bold p-4 border-b">Finished goods</h2>{!data.finished.length?<p className="p-8 text-center text-slate-400">No finished goods yet.</p>:data.finished.map((x:any)=><div key={x.finished_goods_id} className="p-4 border-b flex justify-between"><div><b>{x.product?.product_code} · {x.product?.description}</b><p className="text-xs text-slate-500">{x.location?.location_name}</p></div><div className="text-right"><b>{Number(x.quantity).toLocaleString()} {x.uom?.uom_code}</b><p className="text-xs text-emerald-700">{x.status}</p></div></div>)}</section></div>}
export default function Packing(){return <FactoryGuard><FactoryShell><Body/></FactoryShell></FactoryGuard>}
