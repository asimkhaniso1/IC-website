import{useCallback,useEffect,useState,type FormEvent}from'react';import{CheckCircle2,ClipboardCheck,Loader2}from'lucide-react';import FactoryGuard from'./FactoryGuard';import FactoryShell from'./FactoryShell';import{listStockTakeData,postStockTake}from'./api';const field='w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm';
function Body(){const[data,setData]=useState<any>({products:[],materials:[],uoms:[],locations:[],counts:[]}),[kind,setKind]=useState('material'),[busy,setBusy]=useState(false),[loading,setLoading]=useState(true),[error,setError]=useState(''),[notice,setNotice]=useState('');const load=useCallback(async()=>{setLoading(true);try{setData(await listStockTakeData())}catch(e){setError(e instanceof Error?e.message:'Could not load stock takes.')}finally{setLoading(false)}},[]);useEffect(()=>{void load()},[load]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=e.currentTarget,d=new FormData(f),item=String(d.get('item'));setBusy(true);setError('');setNotice('');try{await postStockTake({p_product:kind==='product'?item:null,p_material:kind==='material'?item:null,p_uom:d.get('uom'),p_location:d.get('location'),p_ownership:d.get('ownership'),p_category:d.get('category'),p_physical:Number(d.get('physical')),p_counted_by:d.get('counter'),p_reason:d.get('reason')});f.reset();setNotice('Physical count posted and variance reconciled.');await load()}catch(x){setError(x instanceof Error?x.message:'Could not post stock take.')}finally{setBusy(false)}}const items=kind==='product'?data.products:data.materials;return <div className="space-y-5"><div><p className="text-xs font-bold text-brand-600 uppercase tracking-[.18em]">Inventory control</p><h1 className="text-2xl font-bold mt-1">Stock Take</h1><p className="text-sm text-slate-500 mt-1">Record physical counts and reconcile variances against the append-only ledger.</p></div>{error&&<div className="p-3 bg-red-50 text-red-700 rounded-xl text-sm">{error}</div>}{notice&&<div className="p-3 bg-emerald-50 text-emerald-700 rounded-xl text-sm flex gap-2"><CheckCircle2 className="w-4 h-4"/>{notice}</div>}<form onSubmit={submit} className="bg-white border rounded-2xl p-5 space-y-4"><div className="flex gap-2 items-center"><ClipboardCheck className="w-5 h-5 text-brand-600"/><h2 className="font-bold">Post physical count</h2></div><div className="grid md:grid-cols-2 xl:grid-cols-4 gap-3"><select value={kind} onChange={e=>setKind(e.target.value)} className={field}><option value="material">Raw material</option><option value="product">Product / finished goods</option></select><select required name="item" className={field}><option value="">Select item…</option>{items.map((x:any)=><option key={x.product_id??x.material_id} value={x.product_id??x.material_id}>{x.product_code??x.material_code} · {x.description??x.material_name}</option>)}</select><select required name="location" className={field}><option value="">Location…</option>{data.locations.map((x:any)=><option key={x.location_id} value={x.location_id}>{x.location_name}</option>)}</select><select required name="uom" className={field}><option value="">Unit…</option>{data.uoms.map((x:any)=><option key={x.uom_id} value={x.uom_id}>{x.uom_code}</option>)}</select><select required name="ownership" className={field}><option value="INTERCONVERTERS_OWNED">Interconverters owned</option><option value="CUSTOMER_OWNED">Customer owned</option><option value="TOLL_MANUFACTURING">Toll manufacturing</option><option value="CONSIGNMENT">Consignment</option><option value="OTHER">Other</option></select><select required name="category" className={field}><option value={kind==='product'?'FINISHED_GOODS':'RAW_MATERIAL'}>{kind==='product'?'Finished goods':'Raw material'}</option><option value="PACKAGING">Packaging</option><option value="WIP">Work in progress</option><option value="SCRAP">Scrap</option><option value="REJECT">Reject</option></select><input required name="physical" type="number" min="0" step=".01" placeholder="Physical quantity" className={field}/><input required name="counter" placeholder="Counted by" className={field}/></div><textarea required name="reason" placeholder="Count reason / authorization reference" className={field}/><button disabled={busy} className="px-5 py-2.5 rounded-xl bg-brand-600 text-white text-sm font-bold disabled:opacity-40">{busy?<Loader2 className="w-4 h-4 animate-spin"/>:'Post stock take'}</button></form><section className="bg-white border rounded-2xl overflow-hidden"><h2 className="font-bold p-4 border-b">Count register</h2>{loading?<div className="p-16 flex justify-center"><Loader2 className="animate-spin"/></div>:!data.counts.length?<p className="p-12 text-center text-slate-400">No physical counts posted.</p>:<div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-slate-50 text-left text-xs text-slate-500"><tr><th className="p-3">Count</th><th className="p-3">Item</th><th className="p-3">Location</th><th className="p-3">Book</th><th className="p-3">Physical</th><th className="p-3">Variance</th><th className="p-3">Counted by</th></tr></thead><tbody>{data.counts.map((x:any)=>{const i=x.product??x.material;return <tr key={x.stock_take_id} className="border-t"><td className="p-3"><b className="font-mono text-xs">{x.stock_take_no}</b><p className="text-xs text-slate-500">{x.count_date}</p></td><td className="p-3"><b>{i?.product_code??i?.material_code}</b><p className="text-xs text-slate-500">{i?.description??i?.material_name}</p></td><td className="p-3">{x.location?.location_name}</td><td className="p-3 font-mono">{Number(x.book_quantity).toLocaleString()}</td><td className="p-3 font-mono">{Number(x.physical_quantity).toLocaleString()}</td><td className={`p-3 font-mono font-bold ${Number(x.variance)===0?'text-emerald-700':'text-amber-700'}`}>{Number(x.variance)>0?'+':''}{Number(x.variance).toLocaleString()} {x.uom?.uom_code}</td><td className="p-3">{x.counted_by}</td></tr>})}</tbody></table></div>}</section></div>}
export default function StockTake(){return <FactoryGuard><FactoryShell><Body/></FactoryShell></FactoryGuard>}
