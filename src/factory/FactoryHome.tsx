import {ArrowRight,Boxes,ClipboardCheck,Factory,FilePlus2,PackageCheck,ShieldCheck,ShoppingCart,Truck} from 'lucide-react';
import {Link} from 'react-router-dom';import FactoryGuard from './FactoryGuard';import FactoryShell from './FactoryShell';
const groups=[['Customers / Sales',[['Order booking','/factory/setup',FilePlus2],['Design Studio','/studio',ClipboardCheck],['Confirmed order','/factory/production',PackageCheck]]],['Production',[['Production order','/factory/production',ClipboardCheck],['Machine assignment','/factory/production',Factory],['Daily production','/factory/production',Factory],['Quality control','#',ShieldCheck]]],['Inventory / Store',[['Goods receiving','/factory/grn',Truck],['Stock register','/factory/stock',Boxes],['Finished goods','#',PackageCheck]]],['Purchasing',[['Purchase request','/factory/purchase-requests',ShoppingCart],['Supplier materials','/factory/grn',Boxes]]]] as const;
function Home(){return <div className="space-y-5"><div><p className="text-xs font-bold text-brand-600 uppercase tracking-[.18em]">Manufacturing management</p><h1 className="text-2xl font-bold mt-1">Factory process map</h1><p className="text-sm text-slate-500 mt-1">Follow the operating flow from customer order through production and inventory.</p></div><div className="grid xl:grid-cols-2 gap-4">{groups.map(([title,items])=><section key={title} className="bg-white border rounded-2xl p-5"><h2 className="text-xs font-bold uppercase tracking-wider text-brand-700 border-b pb-3">{title}</h2><div className="flex gap-2 pt-4">{items.map(([label,to,Icon],i)=><div key={label} className="flex items-center gap-2 flex-1"><Link to={to} className={`w-full min-h-28 rounded-xl border p-3 flex flex-col items-center justify-center text-center gap-2 ${to==='#'?'pointer-events-none bg-slate-50 text-slate-400':'hover:bg-brand-50 hover:border-brand-300'}`}><Icon className="w-7 h-7"/><span className="text-xs font-bold">{label}</span>{to==='#'&&<small>Later phase</small>}</Link>{i<items.length-1&&<ArrowRight className="w-4 h-4 text-slate-300 shrink-0"/>}</div>)}</div></section>)}</div></div>}
export default function FactoryHome(){return <FactoryGuard><FactoryShell><Home/></FactoryShell></FactoryGuard>}
