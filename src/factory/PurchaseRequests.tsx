import{useCallback,useEffect,useState,type FormEvent}from'react';import{AlertCircle,CheckCircle2,Loader2,Plus,ShoppingCart}from'lucide-react';import FactoryGuard from'./FactoryGuard';import FactoryShell from'./FactoryShell';import{advancePurchaseRequest,createPurchaseRequest,listPurchaseRequests}from'./api';
const field='w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none focus:ring-2 focus:ring-brand-200';const btn='rounded-xl bg-brand-600 px-4 py-2.5 text-sm font-bold text-white disabled:opacity-40';type Line={item_description:string;quantity:string;uom:string;rate:string;remarks:string};const blank=():Line=>({item_description:'',quantity:'',uom:'PCS',rate:'',remarks:''});
function Body(){const[rows,setRows]=useState<any[]>([]),[lines,setLines]=useState<Line[]>([blank()]),[busy,setBusy]=useState(false),[error,setError]=useState(''),[notice,setNotice]=useState('');const load=useCallback(async()=>{try{setRows(await listPurchaseRequests())}catch(e){setError(e instanceof Error?e.message:'Could not load requests.')}},[]);useEffect(()=>{void load()},[load]);async function run(fn:()=>Promise<unknown>,msg:string){setBusy(true);setError('');setNotice('');try{await fn();setNotice(msg);await load()}catch(e){setError(e instanceof Error?e.message:'Action failed.')}finally{setBusy(false)}}function setLine(i:number,k:keyof Line,v:string){setLines(x=>x.map((r,n)=>n===i?{...r,[k]:v}:r))}async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=e.currentTarget,d=new FormData(f);await run(()=>createPurchaseRequest({requestedBy:String(d.get('requested')),department:String(d.get('department')),purpose:String(d.get('purpose')),remarks:String(d.get('remarks')||''),lines:lines.map(x=>({...x,quantity:Number(x.quantity),rate:x.rate?Number(x.rate):null}))}),'Purchase request submitted.');f.reset();setLines([blank()])}
return <div className="space-y-5"><div><p className="text-xs font-bold text-brand-600 uppercase tracking-[.18em]">IC-FM-04</p><h1 className="text-2xl font-bold mt-1">Purchase requests</h1><p className="text-sm text-slate-500 mt-1">Prepared, checked, preapproved, and approved as distinct stages.</p></div>{error&&<div className="p-3 bg-red-50 text-red-700 rounded-xl flex gap-2 text-sm"><AlertCircle className="w-4 h-4"/>{error}</div>}{notice&&<div className="p-3 bg-emerald-50 text-emerald-700 rounded-xl flex gap-2 text-sm"><CheckCircle2 className="w-4 h-4"/>{notice}</div>}<form onSubmit={submit} className="bg-white border rounded-2xl p-5 space-y-3"><div className="flex gap-2"><ShoppingCart className="w-5 h-5 text-brand-600"/><h2 className="font-bold">New request</h2></div><div className="grid md:grid-cols-4 gap-2"><input required name="requested" placeholder="Requested by" className={field}/><input required name="department" placeholder="Department" className={field}/><input required name="purpose" placeholder="Purpose" className={`${field} md:col-span-2`}/><input name="remarks" placeholder="Remarks" className={`${field} md:col-span-4`}/></div>{lines.map((x,i)=><div key={i} className="grid md:grid-cols-[2fr_.7fr_.7fr_1fr_2fr] gap-2 bg-slate-50 p-3 rounded-xl"><input required value={x.item_description} onChange={e=>setLine(i,'item_description',e.target.value)} placeholder="Item description" className={field}/><input required type="number" min=".01" step=".01" value={x.quantity} onChange={e=>setLine(i,'quantity',e.target.value)} placeholder="Qty" className={field}/><input required value={x.uom} onChange={e=>setLine(i,'uom',e.target.value)} placeholder="UOM" className={field}/><input type="number" min="0" step=".01" value={x.rate} onChange={e=>setLine(i,'rate',e.target.value)} placeholder="Rate" className={field}/><input value={x.remarks} onChange={e=>setLine(i,'remarks',e.target.value)} placeholder="Remarks" className={field}/></div>)}<div className="flex justify-between"><button type="button" onClick={()=>setLines(v=>[...v,blank()])} className="px-4 py-2 rounded-xl border text-sm font-bold"><Plus className="w-4 h-4 inline"/> Add line</button><button disabled={busy} className={btn}>Submit request</button></div></form><section className="bg-white border rounded-2xl overflow-hidden"><h2 className="font-bold p-4 border-b">Approval queue</h2>{!rows.length?<p className="p-8 text-center text-sm text-slate-400">No requests yet.</p>:rows.map(r=><div key={r.purchase_request_id} className="p-4 border-b last:border-0 space-y-3"><div className="flex justify-between"><div><b className="font-mono text-xs">{r.purchase_request_no}</b><p className="text-sm font-semibold mt-1">{r.purpose}</p></div><span className="text-xs font-bold text-brand-700">{r.status}</span></div><div className="grid grid-cols-4 gap-2">{['PREPARED','CHECKED','PREAPPROVED','APPROVED'].map((x,i)=>{const a=r.approvals?.find((z:any)=>z.approval_level===x);return <div key={x} className={`p-2 rounded-lg border ${a?'bg-emerald-50':'bg-slate-50'}`}><p className="text-[9px] font-bold">{i+1}. {x}</p><p className="text-xs mt-1">{a?.approver_name??'Pending'}</p></div>})}</div>{r.status!=='APPROVED'&&<form onSubmit={e=>{e.preventDefault();const d=new FormData(e.currentTarget);void run(()=>advancePurchaseRequest(r.purchase_request_id,String(d.get('name')),String(d.get('notes')||'')),'Approval stage recorded.')}} className="flex gap-2"><input required name="name" placeholder="Approver name" className={field}/><input name="notes" placeholder="Approval notes" className={field}/><button disabled={busy} className={`${btn} whitespace-nowrap`}>Record next stage</button></form>}</div>)}</section></div>}
export default function PurchaseRequests(){return <FactoryGuard><FactoryShell><Body/></FactoryShell></FactoryGuard>}
